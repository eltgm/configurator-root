openapi: 3.0.3
info:
  title: Configurator Backend API
  version: 1.1.1
  description: API спецификация локального приложения Configurator

servers:
  - url: http://localhost:8080

security:
  - BearerAuth: [ ]

components:
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  schemas:

    ErrorResponse:
      type: object
      properties:
        timestamp:
          type: string
          format: date-time
        status:
          type: integer
        error:
          type: string
        code:
          $ref: '#/components/schemas/ApiErrorCode'
        message:
          type: string
        path:
          type: string
        details:
          type: array
          description: Structured error details; empty when the error has no additional details
          items:
            $ref: '#/components/schemas/ApiErrorDetail'
      required:
        - timestamp
        - status
        - error
        - code
        - message
        - path
        - details

    ApiErrorCode:
      type: string
      description: Stable machine-readable error category
      enum:
        - BUSINESS_ERROR
        - INTERNAL_ERROR
        - NOT_FOUND
        - ENTITY_ALREADY_EXISTS
        - ENTITY_HAS_RELATED_ENTITIES
        - COMPONENT_ARCHIVED
        - CONFIGURATION_CONFLICT
        - VALIDATION_ERROR
        - IMAGE_TOO_LARGE
        - UNSUPPORTED_IMAGE_FORMAT
        - EXTERNAL_STORAGE_UNAVAILABLE

    ApiErrorDetail:
      type: object
      properties:
        field:
          type: string
          nullable: true
          description: Request field or parameter path; absent for object-level errors
        code:
          type: string
          description: Stable detail or validation constraint code in UPPER_SNAKE_CASE
        message:
          type: string
          description: Human-readable detail without rejected values or internal diagnostics
      required:
        - code
        - message

    ### Pagination ###
    DomainPage:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/Domain'
        page:
          type: integer
          minimum: 0
        size:
          type: integer
          minimum: 1
        totalItems:
          type: integer
          minimum: 0
      required: [ items, page, size, totalItems ]

    ComponentPage:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/Component'
        page:
          type: integer
          minimum: 0
        size:
          type: integer
          minimum: 1
        totalItems:
          type: integer
          minimum: 0
      required: [ items, page, size, totalItems ]

    ConfigurationPage:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/SavedConfiguration'
        page:
          type: integer
          minimum: 0
        size:
          type: integer
          minimum: 1
        totalItems:
          type: integer
          minimum: 0
      required: [ items, page, size, totalItems ]

    ### AUTH ###
    User:
      type: object
      properties:
        id:
          type: integer
        email:
          type: string
          format: email
        displayName:
          type: string
          nullable: true
      required:
        - id
        - email

    RegisterRequest:
      type: object
      properties:
        email:
          type: string
          format: email
        password:
          type: string
          minLength: 6
        displayName:
          type: string
          nullable: true
      required:
        - email
        - password

    LoginRequest:
      type: object
      properties:
        email:
          type: string
          format: email
        password:
          type: string
      required:
        - email
        - password

    LoginResponse:
      type: object
      properties:
        token:
          type: string
        user:
          $ref: '#/components/schemas/User'
      required:
        - token
        - user

    ### DOMAIN ###
    Domain:
      type: object
      properties:
        id:
          type: integer
          format: int64
        name:
          type: string
        description:
          type: string
          nullable: true
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - name
        - createdAt

    CreateDomainRequest:
      type: object
      properties:
        name:
          type: string
        description:
          type: string
          nullable: true
      required:
        - name

    UpdateDomainRequest:
      type: object
      properties:
        name:
          type: string
        description:
          type: string
          nullable: true
      required:
        - name

    ### COMPONENT TYPE ###
    ComponentType:
      type: object
      properties:
        id:
          type: integer
          format: int64
        domainId:
          type: integer
          format: int64
        name:
          type: string
        code:
          type: string
          nullable: true
        description:
          type: string
          nullable: true
        orderIndex:
          type: integer
          nullable: true
      required:
        - id
        - domainId
        - name

    CreateComponentTypeRequest:
      type: object
      properties:
        name:
          type: string
        code:
          type: string
          nullable: true
        description:
          type: string
          nullable: true
        orderIndex:
          type: integer
          nullable: true
      required:
        - name

    ### ATTRIBUTE DEFINITION ###
    AttributeDefinition:
      type: object
      properties:
        id:
          type: integer
          format: int64
        domainId:
          type: integer
          format: int64
        componentTypeId:
          type: integer
          format: int64
          nullable: true
        name:
          type: string
        label:
          type: string
        dataType:
          type: string
          enum: [ STRING, NUMBER, BOOLEAN, ENUM ]
        enumValues:
          type: array
          items:
            type: string
          nullable: true
        isRequired:
          type: boolean
          nullable: true
        orderIndex:
          type: integer
          nullable: true
        componentTypeIds:
          type: array
          description: Component types currently linked to this catalog definition
          items:
            type: integer
            format: int64
      required:
        - id
        - domainId
        - name
        - label
        - dataType

    CreateAttributeDefinitionRequest:
      type: object
      properties:
        name:
          type: string
        label:
          type: string
        dataType:
          type: string
          enum: [ STRING, NUMBER, BOOLEAN, ENUM ]
        enumValues:
          type: array
          items:
            type: string
          nullable: true
        isRequired:
          type: boolean
          default: false
        orderIndex:
          type: integer
          nullable: true
      required:
        - name
        - label
        - dataType

    ComponentTypeAttributeSettingsRequest:
      type: object
      properties:
        isRequired:
          type: boolean
          default: false
        orderIndex:
          type: integer
          nullable: true

    ### ATTRIBUTE VALUE ###
    AttributeValueInput:
      type: object
      properties:
        attributeDefinitionId:
          type: integer
          format: int64
        value:
          type: string
      required:
        - attributeDefinitionId
        - value

    AttributeValue:
      type: object
      properties:
        attributeDefinitionId:
          type: integer
          format: int64
        name:
          type: string
        label:
          type: string
        dataType:
          type: string
          enum: [ STRING, NUMBER, BOOLEAN, ENUM ]
        value:
          type: string
          nullable: true
      required:
        - attributeDefinitionId
        - name
        - label
        - dataType

    ### COMPONENT IMAGE ###
    ComponentImage:
      type: object
      properties:
        id:
          type: integer
          format: int64
        url:
          type: string
          description: Relative backend URL for retrieving the image content
          example: /component-images/42/content
        orderIndex:
          type: integer
          nullable: true
      required:
        - id
        - url

    ReorderComponentImagesRequest:
      type: object
      description: Full ordered set of image identifiers currently attached to the component
      properties:
        imageIds:
          type: array
          description: Image identifiers in the desired display order; an empty array is valid for an empty gallery
          items:
            type: integer
            format: int64
            minimum: 1
      required:
        - imageIds

    ### COMPONENT ###
    Component:
      type: object
      properties:
        id:
          type: integer
          format: int64
        componentTypeId:
          type: integer
          format: int64
        name:
          type: string
        brand:
          type: string
          nullable: true
        description:
          type: string
          nullable: true
        archived:
          type: boolean
        createdAt:
          type: string
          format: date-time
        attributes:
          type: array
          items:
            $ref: '#/components/schemas/AttributeValue'
        images:
          type: array
          items:
            $ref: '#/components/schemas/ComponentImage'
      required:
        - id
        - componentTypeId
        - name
        - archived
        - createdAt

    CreateComponentRequest:
      type: object
      properties:
        componentTypeId:
          type: integer
          format: int64
        name:
          type: string
          minLength: 1
          pattern: '^\S.*'
        brand:
          type: string
          nullable: true
        description:
          type: string
          nullable: true
        attributes:
          type: array
          items:
            $ref: '#/components/schemas/AttributeValueInput'
          nullable: true
      required:
        - componentTypeId
        - name

    UpdateComponentRequest:
      type: object
      properties:
        componentTypeId:
          type: integer
          format: int64
        name:
          type: string
          minLength: 1
          pattern: '^\S.*'
        brand:
          type: string
          nullable: true
        description:
          type: string
          nullable: true
        attributes:
          type: array
          items:
            $ref: '#/components/schemas/AttributeValueInput'
      required:
        - componentTypeId
        - name
        - attributes

    ### COMPATIBILITY ###
    CompatibilityLink:
      type: object
      properties:
        id:
          type: integer
          format: int64
        domainId:
          type: integer
          format: int64
        componentAId:
          type: integer
          format: int64
        componentBId:
          type: integer
          format: int64
        comment:
          type: string
          nullable: true
      required:
        - id
        - domainId
        - componentAId
        - componentBId

    CreateCompatibilityLinkRequest:
      type: object
      properties:
        componentAId:
          type: integer
          format: int64
          minimum: 1
        componentBId:
          type: integer
          format: int64
          minimum: 1
        comment:
          type: string
          maxLength: 1000
          nullable: true
          description: Optional comment; blank values are normalized to null
      required:
        - componentAId
        - componentBId

    CompatibilityRuleOperator:
      type: string
      description: Comparison applied to the left and right attribute values
      enum:
        - EQUALS
        - NOT_EQUALS
        - GT
        - GTE
        - LT
        - LTE

    CompatibilityRuleConditionInput:
      type: object
      properties:
        leftAttributeDefinitionId:
          type: integer
          format: int64
          minimum: 1
        operator:
          $ref: '#/components/schemas/CompatibilityRuleOperator'
        rightAttributeDefinitionId:
          type: integer
          format: int64
          minimum: 1
        orderIndex:
          type: integer
          format: int32
          minimum: 0
          nullable: true
          description: Defaults to the condition position in the request
      required:
        - leftAttributeDefinitionId
        - operator
        - rightAttributeDefinitionId

    SaveCompatibilityRuleSetRequest:
      type: object
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 255
          pattern: '.*\S.*'
          description: Human-readable name; surrounding whitespace is removed
        componentTypeAId:
          type: integer
          format: int64
          minimum: 1
        componentTypeBId:
          type: integer
          format: int64
          minimum: 1
        enabled:
          type: boolean
        conditions:
          type: array
          minItems: 1
          items:
            $ref: '#/components/schemas/CompatibilityRuleConditionInput'
      required:
        - name
        - componentTypeAId
        - componentTypeBId
        - enabled
        - conditions

    CompatibilityRuleCondition:
      type: object
      properties:
        id:
          type: integer
          format: int64
        ruleSetId:
          type: integer
          format: int64
        leftAttributeDefinitionId:
          type: integer
          format: int64
        operator:
          $ref: '#/components/schemas/CompatibilityRuleOperator'
        rightAttributeDefinitionId:
          type: integer
          format: int64
        orderIndex:
          type: integer
          format: int32
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - ruleSetId
        - leftAttributeDefinitionId
        - operator
        - rightAttributeDefinitionId
        - orderIndex
        - createdAt

    CompatibilityRuleSet:
      type: object
      properties:
        id:
          type: integer
          format: int64
        domainId:
          type: integer
          format: int64
        name:
          type: string
        componentTypeAId:
          type: integer
          format: int64
        componentTypeBId:
          type: integer
          format: int64
        enabled:
          type: boolean
        conditions:
          type: array
          items:
            $ref: '#/components/schemas/CompatibilityRuleCondition'
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - domainId
        - name
        - componentTypeAId
        - componentTypeBId
        - enabled
        - conditions
        - createdAt

    ### GRAPH ###
    GraphNode:
      type: object
      description: Active component represented as a graph node
      properties:
        id:
          type: integer
          format: int64
        name:
          type: string
        componentTypeId:
          type: integer
          format: int64
        componentTypeName:
          type: string
        brand:
          type: string
          nullable: true
      required:
        - id
        - name
        - componentTypeId
        - componentTypeName

    GraphEdge:
      type: object
      description: Compatibility link between two active components of the domain
      properties:
        id:
          type: integer
          format: int64
        source:
          type: integer
          format: int64
        target:
          type: integer
          format: int64
        comment:
          type: string
          nullable: true
          description: Compatibility link comment
      required:
        - id
        - source
        - target

    GraphResponse:
      type: object
      description: Compatibility graph containing all active domain components, including isolated nodes
      properties:
        nodes:
          type: array
          items:
            $ref: '#/components/schemas/GraphNode'
        edges:
          type: array
          items:
            $ref: '#/components/schemas/GraphEdge'
      required:
        - nodes
        - edges

    ### CONFIGURATOR ###
    ComponentShort:
      type: object
      properties:
        id:
          type: integer
          format: int64
        name:
          type: string
        brand:
          type: string
          nullable: true
        componentTypeId:
          type: integer
          format: int64
      required:
        - id
        - name
        - componentTypeId

    CompatibilityExplanationSource:
      type: string
      description: Source that made the candidate component compatible
      enum:
        - MANUAL
        - AUTOMATIC
        - TRANSITIVE

    CompatibilityConditionExplanation:
      type: object
      description: Successful attribute comparison from an automatic compatibility rule
      properties:
        leftAttributeDefinitionId:
          type: integer
          format: int64
        leftAttributeName:
          type: string
        leftValue:
          type: string
        operator:
          $ref: '#/components/schemas/CompatibilityRuleOperator'
        rightAttributeDefinitionId:
          type: integer
          format: int64
        rightAttributeName:
          type: string
        rightValue:
          type: string
      required:
        - leftAttributeDefinitionId
        - leftAttributeName
        - leftValue
        - operator
        - rightAttributeDefinitionId
        - rightAttributeName
        - rightValue

    CompatibilityExplanation:
      type: object
      description: |
        One reason why the candidate is compatible. Manual explanations contain linkId and
        optional comment. Automatic explanations contain ruleSetId, ruleSetName and all
        successful rule conditions. Transitive explanations contain a shortest component path.
      properties:
        source:
          $ref: '#/components/schemas/CompatibilityExplanationSource'
        linkId:
          type: integer
          format: int64
          nullable: true
        comment:
          type: string
          nullable: true
        ruleSetId:
          type: integer
          format: int64
          nullable: true
        ruleSetName:
          type: string
          nullable: true
        conditions:
          type: array
          nullable: true
          items:
            $ref: '#/components/schemas/CompatibilityConditionExplanation'
        pathComponentIds:
          type: array
          nullable: true
          minItems: 3
          description: |
            Ordered shortest path from the base component to a transitively compatible
            component. Present only for TRANSITIVE explanations.
          items:
            type: integer
            format: int64
            minimum: 1
      required:
        - source

    ConfiguratorCompatibleComponent:
      type: object
      properties:
        id:
          type: integer
          format: int64
        name:
          type: string
        brand:
          type: string
          nullable: true
        componentTypeId:
          type: integer
          format: int64
        explanations:
          type: array
          description: All manual links and enabled automatic rule sets that matched
          items:
            $ref: '#/components/schemas/CompatibilityExplanation'
      required:
        - id
        - name
        - componentTypeId
        - explanations

    ConfiguratorTypeGroup:
      type: object
      properties:
        componentTypeId:
          type: integer
          format: int64
        componentTypeName:
          type: string
        components:
          type: array
          items:
            $ref: '#/components/schemas/ConfiguratorCompatibleComponent'
      required:
        - componentTypeId
        - componentTypeName
        - components

    ConfiguratorResponse:
      type: object
      properties:
        baseComponentId:
          type: integer
          format: int64
        compatibleByType:
          type: array
          items:
            $ref: '#/components/schemas/ConfiguratorTypeGroup'
      required:
        - baseComponentId
        - compatibleByType

    ConfiguratorBatchSearchRequest:
      type: object
      properties:
        componentIds:
          type: array
          minItems: 1
          maxItems: 50
          description: Unique base component identifiers in requested result order
          items:
            type: integer
            format: int64
            minimum: 1
        includeTransitive:
          type: boolean
          default: false
          description: Include components reachable through two or more compatibility edges
      required:
        - componentIds

    ConfiguratorBatchSearchResponse:
      type: object
      properties:
        results:
          type: array
          description: Independent compatibility results in componentIds request order
          items:
            $ref: '#/components/schemas/ConfiguratorResponse'
      required:
        - results

    ConfiguratorIntersectionRequest:
      type: object
      properties:
        componentIds:
          type: array
          minItems: 2
          maxItems: 50
          description: Unique base component identifiers whose compatibility sets are intersected
          items:
            type: integer
            format: int64
            minimum: 1
        includeTransitive:
          type: boolean
          default: false
          description: Include components reachable through two or more compatibility edges
      required:
        - componentIds

    ConfiguratorBaseCompatibility:
      type: object
      properties:
        baseComponentId:
          type: integer
          format: int64
        explanations:
          type: array
          description: Reasons why the candidate is compatible with this base component
          items:
            $ref: '#/components/schemas/CompatibilityExplanation'
      required:
        - baseComponentId
        - explanations

    ConfiguratorIntersectionCompatibleComponent:
      type: object
      properties:
        id:
          type: integer
          format: int64
        name:
          type: string
        brand:
          type: string
          nullable: true
        componentTypeId:
          type: integer
          format: int64
        compatibilityByBase:
          type: array
          description: Compatibility evidence for every selected base component in request order
          items:
            $ref: '#/components/schemas/ConfiguratorBaseCompatibility'
      required:
        - id
        - name
        - componentTypeId
        - compatibilityByBase

    ConfiguratorIntersectionTypeGroup:
      type: object
      properties:
        componentTypeId:
          type: integer
          format: int64
        componentTypeName:
          type: string
        components:
          type: array
          items:
            $ref: '#/components/schemas/ConfiguratorIntersectionCompatibleComponent'
      required:
        - componentTypeId
        - componentTypeName
        - components

    ConfiguratorIntersectionResponse:
      type: object
      properties:
        componentIds:
          type: array
          description: Selected base component identifiers in request order
          items:
            type: integer
            format: int64
        compatibleByType:
          type: array
          description: Components compatible with every selected base component
          items:
            $ref: '#/components/schemas/ConfiguratorIntersectionTypeGroup'
      required:
        - componentIds
        - compatibleByType

    ConfiguratorCandidatesRequest:
      type: object
      properties:
        componentIds:
          type: array
          minItems: 1
          maxItems: 50
          description: >-
            Unique active components currently present in the assembly. When replacing a
            component, the replaced component must be omitted from this list.
          items:
            type: integer
            format: int64
            minimum: 1
      required:
        - componentIds

    ConfiguratorPairStatus:
      type: string
      enum: [ ALLOWED, DENIED, UNKNOWN ]
      description: >-
        ALLOWED means a direct manual or matching automatic relationship exists; DENIED means
        applicable enabled automatic rules exist but none matches; UNKNOWN means no relationship
        knowledge applies to the pair.

    ConfiguratorCandidateStatus:
      type: string
      enum: [ AVAILABLE, BLOCKED, UNRELATED ]
      description: >-
        AVAILABLE candidates have at least one ALLOWED relationship and no DENIED relationship;
        BLOCKED candidates have at least one DENIED relationship; UNRELATED candidates have no
        ALLOWED or DENIED relationship to the assembly.

    ConfiguratorAssemblyStatus:
      type: string
      enum: [ VALID, BLOCKED, DISCONNECTED ]
      description: >-
        VALID means the selected components are connected through ALLOWED direct relationships and
        contain no DENIED pair. BLOCKED means at least one selected pair is DENIED. DISCONNECTED
        means there is no DENIED pair but the ALLOWED graph has more than one component.

    CompatibilityBlockingRule:
      type: object
      properties:
        ruleSetId:
          type: integer
          format: int64
        ruleSetName:
          type: string
      required:
        - ruleSetId
        - ruleSetName

    ConfiguratorCandidateBaseDecision:
      type: object
      properties:
        baseComponentId:
          type: integer
          format: int64
        status:
          $ref: '#/components/schemas/ConfiguratorPairStatus'
        explanations:
          type: array
          description: Positive direct relationship evidence; empty for DENIED and UNKNOWN
          items:
            $ref: '#/components/schemas/CompatibilityExplanation'
        blockingRules:
          type: array
          description: Applicable rules that failed; populated only for DENIED
          items:
            $ref: '#/components/schemas/CompatibilityBlockingRule'
      required:
        - baseComponentId
        - status
        - explanations
        - blockingRules

    ConfiguratorAssemblyPairDecision:
      type: object
      properties:
        leftComponentId:
          type: integer
          format: int64
        rightComponentId:
          type: integer
          format: int64
        status:
          $ref: '#/components/schemas/ConfiguratorPairStatus'
        explanations:
          type: array
          items:
            $ref: '#/components/schemas/CompatibilityExplanation'
        blockingRules:
          type: array
          items:
            $ref: '#/components/schemas/CompatibilityBlockingRule'
      required:
        - leftComponentId
        - rightComponentId
        - status
        - explanations
        - blockingRules

    ConfiguratorAssemblyCandidate:
      type: object
      properties:
        id:
          type: integer
          format: int64
        name:
          type: string
        brand:
          type: string
          nullable: true
        componentTypeId:
          type: integer
          format: int64
        status:
          $ref: '#/components/schemas/ConfiguratorCandidateStatus'
        compatibilityByBase:
          type: array
          description: Pair decisions in componentIds request order
          items:
            $ref: '#/components/schemas/ConfiguratorCandidateBaseDecision'
      required:
        - id
        - name
        - componentTypeId
        - status
        - compatibilityByBase

    ConfiguratorCandidateTypeGroup:
      type: object
      properties:
        componentTypeId:
          type: integer
          format: int64
        componentTypeName:
          type: string
        components:
          type: array
          items:
            $ref: '#/components/schemas/ConfiguratorAssemblyCandidate'
      required:
        - componentTypeId
        - componentTypeName
        - components

    ConfiguratorCandidatesResponse:
      type: object
      properties:
        componentIds:
          type: array
          description: Selected assembly component identifiers in request order
          items:
            type: integer
            format: int64
        candidatesByType:
          type: array
          description: All other active domain components classified for this assembly
          items:
            $ref: '#/components/schemas/ConfiguratorCandidateTypeGroup'
        assemblyStatus:
          $ref: '#/components/schemas/ConfiguratorAssemblyStatus'
        assemblyDecisions:
          type: array
          description: Direct pair decisions for selected components in deterministic pair order
          items:
            $ref: '#/components/schemas/ConfiguratorAssemblyPairDecision'
      required:
        - componentIds
        - candidatesByType
        - assemblyStatus
        - assemblyDecisions

    ### CONFIGURATION ###
    ConfigurationComponent:
      type: object
      description: Current component data included in a saved configuration
      properties:
        id:
          type: integer
          format: int64
        name:
          type: string
        brand:
          type: string
          nullable: true
        componentTypeId:
          type: integer
          format: int64
        componentTypeName:
          type: string
        archived:
          type: boolean
          description: Whether the component was archived after the configuration was saved
      required:
        - id
        - name
        - componentTypeId
        - componentTypeName
        - archived

    SavedConfiguration:
      type: object
      description: |
        Saved user configuration. Components contain their current catalog state and are ordered
        by component type orderIndex, component type name and component id.
      properties:
        id:
          type: integer
          format: int64
        domainId:
          type: integer
          format: int64
        name:
          type: string
          minLength: 1
          maxLength: 255
        description:
          type: string
          nullable: true
          maxLength: 4000
        createdAt:
          type: string
          format: date-time
        components:
          type: array
          items:
            $ref: '#/components/schemas/ConfigurationComponent'
      required:
        - id
        - domainId
        - name
        - createdAt
        - components

    CreateConfigurationRequest:
      type: object
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 255
          description: Name is trimmed by the backend and must remain non-blank
        description:
          type: string
          nullable: true
          maxLength: 4000
          description: Blank description is normalized to null
        componentIds:
          type: array
          minItems: 1
          maxItems: 50
          description: |
            Unique active components from the path domain. At most one component of each type is
            allowed. Every pair must be directly compatible through a manual link or an enabled
            automatic attribute rule. Transitive reachability does not validate a configuration.
          items:
            type: integer
            format: int64
            minimum: 1
      required:
        - name
        - componentIds

    UpdateConfigurationRequest:
      type: object
      description: |
        Complete replacement of the mutable configuration data. Domain, owner and creation time
        are preserved by the backend and cannot be changed by this request.
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 255
          description: Name is trimmed by the backend and must remain non-blank
        description:
          type: string
          nullable: true
          maxLength: 4000
          description: Blank description is normalized to null
        componentIds:
          type: array
          minItems: 1
          maxItems: 50
          description: |
            Complete replacement set of unique active components from the configuration domain.
            At most one component of each type is allowed. Every pair must be directly compatible
            through a manual link or an enabled automatic attribute rule. An archived component is
            rejected even when it already belongs to the saved configuration.
          items:
            type: integer
            format: int64
            minimum: 1
      required:
        - name
        - componentIds

    ConfigurationExport:
      type: object
      description: Versioned, self-contained JSON export of a saved configuration
      properties:
        schemaVersion:
          type: integer
          minimum: 1
          example: 1
        exportedAt:
          type: string
          format: date-time
        configuration:
          $ref: '#/components/schemas/SavedConfiguration'
      required:
        - schemaVersion
        - exportedAt
        - configuration

paths:

  ###################################
  # AUTH
  ###################################

  /auth/register:
    post:
      tags: [ Auth ]
      summary: Register user
      operationId: postAuthRegister
      security: [ ]   # публичный эндпоинт
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegisterRequest'
      responses:
        '201':
          description: Successful registration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: Email already exists
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /auth/login:
    post:
      tags: [ Auth ]
      summary: Login user
      operationId: postAuthLogin
      security: [ ]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginRequest'
      responses:
        '200':
          description: Successful login
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LoginResponse'
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Invalid credentials
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  ###################################
  # DOMAINS
  ###################################

  /domains:
    get:
      tags: [ Domains ]
      summary: List domains (paged)
      operationId: getDomains
      parameters:
        - name: page
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
        - name: size
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 10
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DomainPage'

    post:
      tags: [ Domains ]
      summary: Create domain
      operationId: postDomains
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateDomainRequest'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Domain'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: Domain name conflict
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /domains/demo:
    post:
      tags: [ Domains ]
      summary: Create the complete demo PC domain
      operationId: postDomainsDemo
      description: |
        Atomically creates the deterministic demo catalog, compatibility data and saved
        configuration. Repeated creation returns a domain-name conflict.
      responses:
        '201':
          description: Demo domain created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Domain'
        '409':
          description: Demo domain already exists
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /domains/{id}:
    get:
      tags: [ Domains ]
      summary: Get domain
      operationId: getDomainsById
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: Domain found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Domain'
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

    put:
      tags: [ Domains ]
      summary: Update domain
      operationId: putDomainsById
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateDomainRequest'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Domain'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

    delete:
      tags: [ Domains ]
      summary: Delete domain
      operationId: deleteDomainsById
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '204':
          description: Deleted
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: Domain has dependent entities
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  ###################################
  # COMPONENT TYPES
  ###################################

  /domains/{id}/component-types:
    get:
      tags: [ Component Types ]
      summary: List component types of a domain
      operationId: getDomainsByIdComponentTypes
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ComponentType'
        '404':
          description: Domain not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

    post:
      tags: [ Component Types ]
      summary: Create component type in domain
      operationId: postDomainsByIdComponentTypes
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateComponentTypeRequest'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ComponentType'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Domain not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: ComponentType name conflict
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /component-types/{id}:
    get:
      tags: [ Component Types ]
      summary: Get component type
      operationId: getComponentTypesById
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ComponentType'
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

    put:
      tags: [ Component Types ]
      summary: Update component type
      operationId: putComponentTypesById
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateComponentTypeRequest'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ComponentType'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

    delete:
      tags: [ Component Types ]
      summary: Delete component type
      operationId: deleteComponentTypesById
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '204':
          description: Deleted
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  ###################################
  # ATTRIBUTE DEFINITIONS
  ###################################

  /domains/{domainId}/attributes:
    get:
      tags: [ Attributes ]
      summary: List catalog attribute definitions of a domain
      operationId: getDomainsByDomainIdAttributes
      parameters:
        - name: domainId
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/AttributeDefinition'
        '404':
          description: Domain not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

    post:
      tags: [ Attributes ]
      summary: Create a catalog attribute definition in a domain
      operationId: postDomainsByDomainIdAttributes
      parameters:
        - name: domainId
          in: path
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateAttributeDefinitionRequest'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AttributeDefinition'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Domain not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /component-types/{id}/attributes:
    get:
      tags: [ Attributes ]
      summary: List attribute definitions of a component type
      operationId: getComponentTypesByIdAttributes
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/AttributeDefinition'
        '404':
          description: ComponentType not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

    post:
      tags: [ Attributes ]
      summary: Create attribute definition for a component type
      operationId: postComponentTypesByIdAttributes
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateAttributeDefinitionRequest'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AttributeDefinition'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: ComponentType not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: Attribute name conflict
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /component-types/{componentTypeId}/attributes/{attributeId}:
    put:
      tags: [ Attributes ]
      summary: Attach a catalog attribute to a component type or update link settings
      operationId: putComponentTypesByComponentTypeIdAttributesByAttributeId
      parameters:
        - name: componentTypeId
          in: path
          required: true
          schema:
            type: integer
            format: int64
        - name: attributeId
          in: path
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ComponentTypeAttributeSettingsRequest'
      responses:
        '200':
          description: Attached or updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AttributeDefinition'
        '400':
          description: Attribute and component type belong to different domains
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Component type or attribute not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: Attribute name conflict in component type
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

    delete:
      tags: [ Attributes ]
      summary: Detach an attribute from a component type and delete values of that type
      operationId: deleteComponentTypesByComponentTypeIdAttributesByAttributeId
      parameters:
        - name: componentTypeId
          in: path
          required: true
          schema:
            type: integer
            format: int64
        - name: attributeId
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '204':
          description: Detached and scoped values deleted
        '404':
          description: Component type, attribute or link not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /attributes/{id}:
    put:
      tags: [ Attributes ]
      summary: Update attribute definition
      operationId: putAttributesById
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateAttributeDefinitionRequest'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AttributeDefinition'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

    delete:
      tags: [ Attributes ]
      summary: Delete a catalog attribute definition, all links and all values
      operationId: deleteAttributesById
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '204':
          description: Deleted
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: Attribute is used by compatibility rules
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  ###################################
  # COMPONENTS
  ###################################

  /domains/{domainId}/components:
    get:
      tags: [ Components ]
      summary: List components of domain (paged)
      operationId: getDomainsByDomainIdComponents
      parameters:
        - name: domainId
          in: path
          required: true
          schema:
            type: integer
            format: int64
        - name: componentTypeId
          in: query
          schema:
            type: integer
            format: int64
        - name: name
          in: query
          schema:
            type: string
        - name: archived
          in: query
          description: |
            Optional archive filter. true returns only archived components, false returns only active
            components, and an omitted parameter preserves the existing unfiltered result.
          schema:
            type: boolean
        - name: page
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
        - name: size
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 10
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ComponentPage'
        '404':
          description: Domain not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /components:
    post:
      tags: [ Components ]
      summary: Create component
      operationId: postComponents
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateComponentRequest'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Component'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: ComponentType not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: ComponentType with name already exists
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /components/{id}:
    get:
      tags: [ Components ]
      summary: Get component details
      operationId: getComponentsById
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Component'
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

    put:
      tags: [ Components ]
      summary: Update component
      operationId: putComponentsById
      description: Fully replaces editable component fields and attribute values. Component type cannot be changed.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
            minimum: 1
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateComponentRequest'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Component'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: Component with the same name already exists within the component type
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

    delete:
      tags: [ Components ]
      summary: Archive component
      operationId: deleteComponentsById
      description: Archives a component without deleting its attributes, images, compatibility links, or configuration references.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
            minimum: 1
      responses:
        '204':
          description: Archived
        '400':
          description: Invalid component identifier
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /components/{id}/restore:
    post:
      tags: [ Components ]
      summary: Restore archived component
      operationId: postComponentsByIdRestore
      description: |
        Idempotently returns a component to the active catalog. An already active component is
        returned unchanged. Attributes, images, compatibility links, and configuration references
        are preserved.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
            minimum: 1
      responses:
        '200':
          description: Component is active
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Component'
        '400':
          description: Invalid component identifier
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Component not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  ###################################
  # COMPONENT IMAGES
  ###################################

  /component-images/{id}:
    delete:
      tags: [ Component Images ]
      summary: Delete component image
      operationId: deleteComponentImagesById
      description: |
        Permanently deletes an image attached to an active component. The object is removed from
        external storage before its metadata is deleted. If object storage is unavailable, metadata
        remains unchanged so deletion can be retried. Images of archived components cannot be deleted.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
            minimum: 1
      responses:
        '204':
          description: Component image deleted
        '400':
          description: Invalid component image identifier
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Component image not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: Parent component is archived
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '503':
          description: External object storage is unavailable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /component-images/{id}/content:
    get:
      tags: [ Component Images ]
      summary: Get component image content
      operationId: getComponentImagesByIdContent
      description: |
        Returns the original JPEG, PNG, or WebP bytes through the backend so object storage can
        remain private. Images attached to archived components remain available. The response is
        never redirected to an object-storage URL.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
            minimum: 1
      responses:
        '200':
          description: Original image content
          headers:
            Cache-Control:
              description: Private response that must be revalidated before reuse
              schema:
                type: string
                example: private, no-cache
            Content-Disposition:
              description: Displays the image inline
              schema:
                type: string
                example: inline
            Content-Length:
              description: Image size in bytes
              schema:
                type: integer
                format: int64
          content:
            image/jpeg:
              schema:
                type: string
                format: binary
            image/png:
              schema:
                type: string
                format: binary
            image/webp:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid component image identifier
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Component image not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '503':
          description: External object storage is unavailable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /components/{id}/images:
    post:
      tags: [ Components ]
      summary: Upload image for component
      operationId: postComponentsByIdImages
      description: |
        Uploads a JPEG, PNG, or WebP image up to 10 MiB to external object storage
        and attaches it to an active component. If orderIndex is omitted, the image
        is placed after all existing component images.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
            minimum: 1
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
                  maxLength: 10485760
                  description: JPEG, PNG, or WebP image with a maximum size of 10 MiB
                orderIndex:
                  type: integer
                  minimum: 0
                  nullable: true
                  description: Optional non-negative display order; defaults to the next available index
              required:
                - file
      responses:
        '201':
          description: Image uploaded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ComponentImage'
        '400':
          description: Invalid file or request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: Component is archived
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '413':
          description: Image exceeds the 10 MiB size limit
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '415':
          description: Unsupported image format
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '503':
          description: External object storage is unavailable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Component not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

    get:
      tags: [ Components ]
      summary: Get images of component
      operationId: getComponentsByIdImages
      description: |
        Returns all images of an existing component, including an archived component.
        An empty array is returned when the component has no images. Images are ordered
        by orderIndex ascending with null values last, then by id ascending.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
            minimum: 1
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ComponentImage'
        '400':
          description: Invalid component identifier
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Component not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /components/{id}/images/order:
    put:
      tags: [ Components ]
      summary: Replace component image order
      operationId: putComponentsByIdImagesOrder
      description: |
        Atomically replaces the display order of all images attached to an active component.
        The request must contain every current image identifier exactly once and no foreign image
        identifiers. The first identifier receives orderIndex 0, the second 1, and so on.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
            minimum: 1
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReorderComponentImagesRequest'
      responses:
        '200':
          description: Image order replaced
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ComponentImage'
        '400':
          description: Invalid identifier list or list does not exactly match current component images
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Component not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: Component is archived
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  ###################################
  # COMPATIBILITY
  ###################################

  /domains/{id}/compatibility:
    post:
      tags: [ Compatibility ]
      summary: Create compatibility link
      operationId: postDomainsByIdCompatibility
      description: |
        Creates one undirected COMPATIBLE link between two active components of the
        specified domain. The component identifiers are normalized before persistence:
        the smaller identifier is stored as componentAId and the larger as componentBId.
        Blank comments are normalized to null.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
            minimum: 1
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateCompatibilityLinkRequest'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CompatibilityLink'
        '400':
          description: Invalid input, self-link, or component outside the specified domain
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Domain or component not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: Duplicate compatibility link or archived component
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /domains/{id}/compatibility/rules:
    get:
      tags: [ Compatibility ]
      summary: Get compatibility rule sets
      operationId: getDomainsByIdCompatibilityRules
      description: Returns all automatic compatibility rule sets of the domain in identifier order.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
            minimum: 1
      responses:
        '200':
          description: Compatibility rule sets
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/CompatibilityRuleSet'
        '400':
          description: Invalid domain identifier
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Domain not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    post:
      tags: [ Compatibility ]
      summary: Create compatibility rule set
      operationId: postDomainsByIdCompatibilityRules
      description: |
        Creates an attribute-to-attribute automatic compatibility rule set. The component-type
        pair is stored in ascending identifier order. When the submitted pair is reversed,
        condition sides and directional operators are reversed as well.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
            minimum: 1
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SaveCompatibilityRuleSetRequest'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CompatibilityRuleSet'
        '400':
          description: Invalid rule set, condition, component type, or attribute
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Domain or component type not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: Rule set with the same name and component-type pair already exists
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /domains/{id}/compatibility/rules/{ruleId}:
    get:
      tags: [ Compatibility ]
      summary: Get compatibility rule set
      operationId: getDomainsByIdCompatibilityRulesByRuleId
      description: Returns a rule set scoped to the specified domain.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
            minimum: 1
        - name: ruleId
          in: path
          required: true
          schema:
            type: integer
            format: int64
            minimum: 1
      responses:
        '200':
          description: Compatibility rule set
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CompatibilityRuleSet'
        '400':
          description: Invalid domain or rule-set identifier
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Domain or compatibility rule set not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    put:
      tags: [ Compatibility ]
      summary: Replace compatibility rule set
      operationId: putDomainsByIdCompatibilityRulesByRuleId
      description: |
        Fully replaces rule-set metadata and conditions while preserving its identifier and
        creation timestamp. The rule set remains scoped to the specified domain.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
            minimum: 1
        - name: ruleId
          in: path
          required: true
          schema:
            type: integer
            format: int64
            minimum: 1
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SaveCompatibilityRuleSetRequest'
      responses:
        '200':
          description: Updated compatibility rule set
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CompatibilityRuleSet'
        '400':
          description: Invalid rule set, condition, component type, or attribute
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Domain, component type, or compatibility rule set not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: Rule set with the same name and component-type pair already exists
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    delete:
      tags: [ Compatibility ]
      summary: Delete compatibility rule set
      operationId: deleteDomainsByIdCompatibilityRulesByRuleId
      description: Physically deletes a rule set and all its conditions within the specified domain.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
            minimum: 1
        - name: ruleId
          in: path
          required: true
          schema:
            type: integer
            format: int64
            minimum: 1
      responses:
        '204':
          description: Deleted
        '400':
          description: Invalid domain or rule-set identifier
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Domain or compatibility rule set not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /domains/{id}/compatibility/{linkId}:
    delete:
      tags: [ Compatibility ]
      summary: Delete compatibility link
      operationId: deleteDomainsByIdCompatibilityByLinkId
      description: |
        Physically deletes a compatibility link scoped to the specified domain.
        A link that belongs to another domain is treated as not found.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
            minimum: 1
        - name: linkId
          in: path
          required: true
          schema:
            type: integer
            format: int64
            minimum: 1
      responses:
        '204':
          description: Deleted
        '400':
          description: Invalid domain or compatibility link identifier
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Domain or compatibility link not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  ###################################
  # GRAPH
  ###################################

  /domains/{id}/compatibility/graph:
    get:
      tags: [ Compatibility ]
      summary: Get compatibility graph of domain
      operationId: getDomainsByIdCompatibilityGraph
      description: >
        Returns all non-archived components of the domain as nodes, including isolated components,
        and compatibility links whose endpoints are both present in the graph.
        Nodes and edges are sorted by identifier in ascending order.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
            minimum: 1
      responses:
        '200':
          description: Compatibility graph
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GraphResponse'
        '400':
          description: Invalid domain identifier
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Domain not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  ###################################
  # CONFIGURATOR
  ###################################

  /domains/{id}/configurator/compatible:
    get:
      tags: [ Configurator ]
      summary: Get compatible components for selected component
      operationId: getDomainsByIdConfiguratorCompatible
      description: |
        Returns active domain components that are directly compatible with the selected active
        component through either a manual compatibility link or at least one enabled automatic
        attribute rule set. All conditions inside one rule set must match; multiple rule sets are
        alternatives. Every returned component contains compatibility explanations. By default,
        only direct compatibility is returned. When includeTransitive=true, active components
        reachable through the manual and automatic compatibility graph are also returned with
        one deterministic shortest path. Results are grouped by component type.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
            minimum: 1
        - name: componentId
          in: query
          required: true
          schema:
            type: integer
            format: int64
            minimum: 1
        - name: includeTransitive
          in: query
          required: false
          description: Include components reachable through two or more compatibility edges
          schema:
            type: boolean
            default: false
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConfiguratorResponse'
        '400':
          description: Invalid identifier, component outside the domain, or archived base component
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Domain or component not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /domains/{id}/configurator/compatible/search:
    post:
      tags: [ Configurator ]
      summary: Search compatibility independently for multiple selected components
      operationId: postDomainsByIdConfiguratorCompatibleSearch
      description: |
        Calculates the same direct or transitive compatibility result as the single-component
        endpoint for every requested active base component. Results preserve componentIds order.
        Sets are not combined or intersected. Domain components, manual links, enabled automatic
        rules and the compatibility graph are loaded once for the whole request.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
            minimum: 1
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConfiguratorBatchSearchRequest'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConfiguratorBatchSearchResponse'
        '400':
          description: Invalid, duplicate, archived, or out-of-domain base component
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Domain or component not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /domains/{id}/configurator/compatible/intersection:
    post:
      tags: [ Configurator ]
      summary: Find components compatible with every selected component
      operationId: postDomainsByIdConfiguratorCompatibleIntersection
      description: |
        Intersects the direct or transitive compatibility sets of all selected active base
        components. Selected base components are excluded from candidates. Each resulting
        component contains compatibility evidence for every base component in componentIds
        request order. Domain components, manual links, enabled automatic rules and the
        compatibility graph are loaded once for the whole request.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
            minimum: 1
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConfiguratorIntersectionRequest'
      responses:
        '200':
          description: Success, including an empty intersection
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConfiguratorIntersectionResponse'
        '400':
          description: Invalid, duplicate, archived, or out-of-domain base component
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Domain or component not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /domains/{id}/configurator/candidates:
    post:
      tags: [ Configurator ]
      summary: Classify candidates for the current assembly
      operationId: postDomainsByIdConfiguratorCandidates
      description: |
        Classifies every other active domain component using direct assembly relationships.
        A candidate is AVAILABLE when it has at least one allowed relationship to a selected
        component and no denied relationship. Any denied relationship makes it BLOCKED.
        Candidates without allowed or denied relationships are UNRELATED. The operation never
        uses transitive graph paths because an assembly must be connected by selected components.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
            minimum: 1
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConfiguratorCandidatesRequest'
      responses:
        '200':
          description: Success, including empty candidate groups
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConfiguratorCandidatesResponse'
        '400':
          description: Invalid, duplicate, archived, or out-of-domain assembly component
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Domain or component not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  ###################################
  # CONFIGURATIONS (SAVED BUILDS)
  ###################################

  /domains/{id}/configurations:
    get:
      tags: [ Configurations ]
      summary: List configurations of domain (paged)
      operationId: getDomainsByIdConfigurations
      description: |
        Returns only configurations owned by the current user. Results are ordered by createdAt
        descending and then by id descending. An empty page is returned when no configurations exist.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
        - name: page
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
        - name: size
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 10
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConfigurationPage'
        '400':
          description: Invalid pagination parameters
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Domain not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

    post:
      tags: [ Configurations ]
      summary: Create configuration
      operationId: postDomainsByIdConfigurations
      description: |
        Atomically saves a non-empty, possibly incomplete build for the current user. Components
        must be active, belong to this domain, have distinct component types and be pairwise
        compatible through manual links or enabled automatic rules. Transitive compatibility is
        not sufficient. The owner is always obtained from the authenticated server context.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateConfigurationRequest'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SavedConfiguration'
        '400':
          description: Invalid input, duplicate component ids or components from different domains
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Domain or component not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: Archived component, repeated component type or incompatible component set
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /configurations/{id}:
    get:
      tags: [ Configurations ]
      summary: Get configuration
      operationId: getConfigurationsById
      description: |
        Returns a configuration owned by the current user. Archived components remain visible and
        are marked with archived=true. A missing or foreign-owned configuration is returned as 404.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SavedConfiguration'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Configuration not found or not owned by the current user
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

    put:
      tags: [ Configurations ]
      summary: Fully update configuration
      operationId: putConfigurationsById
      description: |
        Atomically replaces the name, description and complete component set of a configuration
        owned by the current user. Domain, owner and creation time are preserved. The complete
        resulting component set is validated using the same strict rules as configuration creation:
        all components must be active, belong to the configuration domain, have distinct types and
        be pairwise directly compatible. Transitive compatibility is not sufficient. A missing or
        foreign-owned configuration is returned as 404.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
            minimum: 1
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateConfigurationRequest'
      responses:
        '200':
          description: Updated configuration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SavedConfiguration'
        '400':
          description: Invalid input, duplicate component ids or components from different domains
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Configuration or component not found, or configuration is not owned by the user
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: Archived component, repeated component type or incompatible component set
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

    delete:
      tags: [ Configurations ]
      summary: Permanently delete configuration
      operationId: deleteConfigurationsById
      description: |
        Permanently deletes a configuration owned by the current user. Component links are removed
        together with the configuration, while catalog components and other configurations remain
        unchanged. A missing, foreign-owned or already deleted configuration is returned as 404.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
            minimum: 1
      responses:
        '204':
          description: Configuration deleted
        '400':
          description: Invalid configuration identifier
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Configuration not found or not owned by the current user
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /configurations/{id}/export/json:
    get:
      tags: [ Configurations ]
      summary: Export configuration as JSON
      operationId: getConfigurationsByIdExportJson
      description: |
        Builds a versioned JSON document from the current catalog state. Archived components are
        exported with archived=true. The endpoint is available only to the configuration owner.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: Exported JSON
          headers:
            Content-Disposition:
              description: Download filename based on the configuration id
              schema:
                type: string
                example: attachment; filename="configuration-42.json"
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConfigurationExport'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Configuration not found or not owned by the current user
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
